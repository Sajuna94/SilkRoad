export interface Product {
  id: number;
  name: string;
  price: number;
  description: string;
  img: string;
  sugar: string;
  ice: string;
  quantity: number;
  note: string;
  // sugarOptions?: string[];
  // iceOptions?: string[];
}
